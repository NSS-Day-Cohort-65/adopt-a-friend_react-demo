import React from 'react';
import ReactDOM from 'react-dom/client';
import './index.css';
import { Adopt_A_Friend } from './Adopt_A_Friend';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <Adopt_A_Friend />
  </React.StrictMode>
);